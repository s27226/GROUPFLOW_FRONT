import { Layout } from "../../../components/layout";
import { ProfileTagsEditor } from "../../../components/profile";

export default function ProfileTagsPage() {
    return (
        <Layout variant="compact">
            <ProfileTagsEditor />
        </Layout>
    );
}
